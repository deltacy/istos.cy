<% if locale == :en %>
  <%= link_to "#{t('greek', locale: :el)}", url_for(locale: :el), class: 'btn btn-outline-secondary' %>
<% else %>
  <%= link_to "#{t('english', locale: :en)}", url_for(locale: :en), class: 'btn btn-outline-secondary' %>
<% end %>
